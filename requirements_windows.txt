# Full requirements for Windows deployment + EXE building
# NOTE: webdriver-manager is intentionally excluded.
# Selenium 4.10+ includes selenium-manager which downloads
# the correct Windows chromedriver automatically at runtime.
selenium==4.20.0
pystray==0.19.5
Pillow==10.3.0
plyer==2.1.0
winotify==1.1.0
win10toast==0.9
pyinstaller==6.6.0
